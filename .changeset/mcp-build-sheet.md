---
"@svgrid/mcp": minor
---

A `build_sheet` prompt: scaffold a spreadsheet on the SvSheet shell for a
described workbook, grounded in the shipped docs and demos and verified
with `svgrid_check_code` before it is shown.
