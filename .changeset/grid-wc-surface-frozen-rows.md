---
"@svgrid/grid-wc": patch
---

The element and the React, Vue and Angular wrappers pick up the grid's new
props: `frozenRows`, `editOnSecondClick`, `processCellForFill` and
`processCellFromClipboard`, and its new events `onColumnResize` and
`onRowResize`.
