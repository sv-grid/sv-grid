# sv-grid

> The Svelte 5-native data grid. Headless engine + render component, AI-native, WAI-ARIA, virtualized to 100k rows.

Two npm packages: `@svgrid/grid` (MIT, open source) and `@svgrid/enterprise` (commercial - export, import, pivot, AI helpers).

For the full text of every doc page concatenated: see [llms-full.txt](/llms-full.txt).
For a machine-readable manifest: see [docs.json](/docs.json).

## Overview

- [Changelog](https://svgrid.com/changelog): The user-facing log of what shipped. Format follows Keep a Changelog 1.1.0; versioning follows Semantic Versioning.
- [Getting Started with SvGrid](https://svgrid.com/getting-started-full): SvGrid is a modern, production-ready data grid for Svelte 5 - a headless core engine paired with a Svelte render component (<SvGrid>). It scales from a 10-row read-only table to a virtualized 100,0…
- [Getting started with SvGrid](https://svgrid.com/getting-started): SvGrid is a modern, production-ready data grid for Svelte 5 - a headless core engine paired with a Svelte render component (<SvGrid>). It scales from a 10-row read-only table to a virtualized 100,0…
- [Why headless?](https://svgrid.com/why-headless): SvGrid is **headless at the core**, with a fully-styled Svelte component shipped on top. That two-layer split is deliberate, and worth understanding before you reach for either.

## brand

- [SvGrid - Company / Brand Description](https://svgrid.com/brand/company-description): *About the company and brand behind SvGrid. For partner listings, press kits, marketplace profiles, and procurement forms. Product descriptions live elsewhere.*
- [GitHub social preview images](https://svgrid.com/brand/social/README): On-brand 1280x640 cards for GitHub's repo **Social preview** (Settings -> General -> Social preview). All are PNG, 1280x640 (GitHub's recommended size), and well under the 1 MB upload limit.

## Compliance

- [Audit log integration](https://svgrid.com/compliance/audit-log): A typed, tamper-evident audit trail of every user action in the grid, in under 50 lines. Pattern works for any backend - your existing audit table, Datadog logs, an immutable ledger (DynamoDB with …
- [GDPR + data residency](https://svgrid.com/compliance/gdpr): sv-grid is **GDPR-neutral**: the library never transmits personal data, never stores it server-side, and never logs it. If your app is GDPR-compliant before adding sv-grid, adding sv-grid does not …
- [HIPAA posture](https://svgrid.com/compliance/hipaa): sv-grid is **HIPAA-neutral**: the library does not transmit, store, or process PHI on its own. If your app is HIPAA-compliant before adding sv-grid, adding sv-grid does not break that compliance.
- [Compliance](https://svgrid.com/compliance/index): sv-grid is a client-side UI library - **all data stays in the browser**, the library never makes a network call of its own, no telemetry phones home. The compliance story is therefore short, but be…
- [SOC 2 posture](https://svgrid.com/compliance/soc2): sv-grid is a UI library, not a service - so it cannot itself hold a SOC 2 report. But it can sit inside a SOC 2-audited application, and the controls below describe the line where the library's res…

## Enterprise tier

- [Enterprise evaluation](https://svgrid.com/enterprise/evaluation): The @svgrid/enterprise package is soft-gated; you can evaluate every feature in production-equivalent code paths without contacting sales. This page is the playbook.
- [Enterprise licensing](https://svgrid.com/enterprise/licensing): The @svgrid/enterprise package is **soft-gated**: every feature runs without a key, with a small unlicensed-build watermark in the bottom-right corner of the grid + a one-time console nudge. Set a …
- [Enterprise feature pack](https://svgrid.com/enterprise/README): @svgrid/enterprise is a paid add-on for @svgrid/grid. It bolts onto the same <SvGrid> you already have and adds four feature areas: data export, data import, an AI assistant, and pivot tables.
- [SvGrid Studio](https://svgrid.com/enterprise/studio): Studio is the grid-centric data-app layer in @svgrid/enterprise: turn a database or schema into a **production-ready CRUD screen** - grid, edit form, and SvelteKit API route - in seconds. One Entit…
- [Access control (RBAC)](https://svgrid.com/enterprise/studio/access-control): Studio has **authentication** (who you are, via SvAuthGate + Supabase) and, separately, **authorization** - what a signed-in user may do. Role-based access control (RBAC) gates **screens** and **wr…
- [Accessibility](https://svgrid.com/enterprise/studio/accessibility): A Studio screen is accessible out of the box, because it is built from the grid and the edit form - both designed to the WAI-ARIA patterns. This page is what you get for free, what you own, and how…
- [AI generation](https://svgrid.com/enterprise/studio/ai-generation): The @svgrid/mcp server exposes Studio to AI coding agents (Claude Code, Cursor, Codex, ...) through the Model Context Protocol. Ask your agent to build a screen for a table and it introspects, scaf…
- [Studio API reference](https://svgrid.com/enterprise/studio/api): Every Studio export in one place, grouped by what it does. Each links to the guide with the full detail. All are imported from **@svgrid/enterprise** unless noted; createServerDataSource is from @s…
- [Visual app designer](https://svgrid.com/enterprise/studio/app-designer): SvStudioDesigner is the grid-centric visual **data-app** designer - compose a multi-entity app by arranging data-bound blocks on a canvas, then generate a runnable SvelteKit project. It's the app-l…
- [Audit trail](https://svgrid.com/enterprise/studio/audit-log): An audit trail records **who changed what, and when** - a baseline requirement for any regulated or multi-user app. Studio records writes **server-side**, at the API route, so the trail can't be sk…
- [Auth & secured screens](https://svgrid.com/enterprise/studio/auth): Put a Studio screen behind a login and scope its data per user. The pattern is **Supabase Auth for *who*, Row-Level Security for *what*** - auth establishes the signed-in user; RLS policies decide …
- [Computed fields & hooks](https://svgrid.com/enterprise/studio/business-logic): Two ways to put business logic on an EntitySchema: **computed fields** (values derived from the row) and **hooks** (logic that runs when rows are created, updated, or deleted). Both live on the sch…
- [The Studio CLI](https://svgrid.com/enterprise/studio/cli): @svgrid/studio is the command-line generator. One command reads a schema or a live database and writes a working CRUD screen into your SvelteKit app.
- [Code generation](https://svgrid.com/enterprise/studio/code-generation): Studio generates plain SvelteKit code you own - there is no runtime and no lock-in. The same scaffold() core powers the CLI, the AI generator, and the visual designer, so all three emit identical f…
- [Dashboards](https://svgrid.com/enterprise/studio/dashboards): SvSchemaDashboard renders a **schema-driven dashboard** - KPI tiles + charts - over an entity. It's a declarative data *view*, not a drag-drop page builder: you describe the widgets as plain data a…
- [Data binding](https://svgrid.com/enterprise/studio/data-binding): Every Studio screen binds to data through one small, uniform contract: **ServerDataSource**. Read a page of rows, and (optionally) create, update, and delete. Because sorting, filtering, pagination…
- [Databases](https://svgrid.com/enterprise/studio/databases): Studio connects directly to your relational database, reads a table's columns from the catalog, and scaffolds a connected CRUD screen. Supported: **PostgreSQL, Supabase, MySQL / MariaDB, SQL Server…
- [Deploying a Studio app](https://svgrid.com/enterprise/studio/deployment): A Studio-generated app is a normal SvelteKit app - the screens are +page.svelte files and the data endpoints are +server.ts routes. It deploys anywhere SvelteKit runs. This page is the production c…
- [Visual designer](https://svgrid.com/enterprise/studio/designer): SvSchemaDesigner is the human-in-the-loop surface over the Studio engine. Author an EntitySchema visually, see the grid and edit form update live, and click **Generate code** to emit the same Svelt…
- [Drizzle schema](https://svgrid.com/enterprise/studio/drizzle): If you use Drizzle ORM, Studio can scaffold a screen straight from your schema.ts - no live database connection needed at generation time. Your schema file is the single source of truth.
- [Edit forms & validation](https://svgrid.com/enterprise/studio/edit-forms): SvGridEditPanel is the create / edit form for a row. It renders itself from an EntitySchema, validates input, and hands you a ready payload to save. It presents as a right-hand **drawer**, a center…
- [Getting started](https://svgrid.com/enterprise/studio/getting-started): This is the gentlest path into SvGrid Studio. By the end you will have a real, working **Customers** screen - a grid you can sort, filter, and page, with a create / edit form and delete - running o…
- [Localization (i18n)](https://svgrid.com/enterprise/studio/i18n): Ship a Studio app in more than one language. When localization is on, the generator emits a **message catalog** + a **locale switcher**, and routes the app's user-facing strings - nav labels, scree…
- [In-memory](https://svgrid.com/enterprise/studio/in-memory): The in-memory source runs a full CRUD data screen against a plain array - no database, no API. It applies the grid's filter, global search, sort, and paging in memory, and supports create / update …
- [Launch the designer](https://svgrid.com/enterprise/studio/launch): One command opens the visual app designer in your browser, backed by a tiny local server that **auto-saves** your design to disk and **generates the app** into a folder - no host app to build, no c…
- [Master-detail](https://svgrid.com/enterprise/studio/master-detail): SvGridMasterDetail expands a master row into a nested grid of related records - orders and their line items, customers and their invoices, and so on. It is a feature of the grid's data stack (built…
- [Navigation & row actions](https://svgrid.com/enterprise/studio/navigation): Blocks show data; **navigation** turns a set of screens into an app. Studio wires two kinds of flow, both grid-native: **drill-through** (click a row or a chart category to open another screen, fil…
- [OData & GraphQL](https://svgrid.com/enterprise/studio/odata-graphql): Studio binds to OData and GraphQL the same way it binds to any API: implement the ServerDataSource contract and translate the grid's request (paging + sortModel + filterModel) into the query the se…
- [Tutorial: a Postgres CRUD grid](https://svgrid.com/enterprise/studio/postgres-grid): The **one page** that wires a data grid to a **PostgreSQL** database with full **create / read / update / delete** - sorting, filtering, search, and paging included. SQL runs on the server (a drive…
- [Prisma schema](https://svgrid.com/enterprise/studio/prisma): If you use Prisma, Studio can scaffold screens straight from your schema.prisma - no live database connection needed at generation time. Your Prisma schema is the single source of truth, and the --…
- [Real-time (Supabase)](https://svgrid.com/enterprise/studio/realtime): Make a Studio screen **live**: when a row is inserted, updated, or deleted in the database - by another user, another tab, or a background job - the grid reflects it instantly, with no polling. Thi…
- [Relations & master-detail](https://svgrid.com/enterprise/studio/relations): Real apps have related entities - a company has many deals, an order has many line items. Studio models relations in the EntitySchema and turns them into **master-detail** screens (a row that expan…
- [REST & custom APIs](https://svgrid.com/enterprise/studio/rest-api): Not every backend is a database Studio can introspect. When you have an existing REST API - or any bespoke data layer - back the screen with a ServerDataSource. Use the built-in **createRestDataSou…
- [Tutorial: a REST CRUD grid](https://svgrid.com/enterprise/studio/rest-grid): The **one page** that wires a data grid to an existing **REST / JSON API** with full **create / read / update / delete** - sorting, filtering, search, and paging included. Copy the three files and …
- [Sample apps + bind your data](https://svgrid.com/enterprise/studio/samples): The fastest way to a working app: open a **ready-made sample**, see it running with realistic data, then point it at **your own database**. No block-by-block build, no placeholder rows.
- [The EntitySchema](https://svgrid.com/enterprise/studio/schema): EntitySchema is the single model at the heart of Studio. It describes an entity - its fields, types, validation, keys, and relations - and everything else derives from it: the grid columns, the edi…
- [Sorting, filtering & paging](https://svgrid.com/enterprise/studio/server-grid): In a Studio screen the data lives on the server, so the grid does not sort, filter, or paginate locally - it renders its **native UI** and emits the user's intent, and your data source fetches the …
- [Tutorial: a Supabase CRUD grid](https://svgrid.com/enterprise/studio/supabase-grid): This is the **one page** that does it all: create a data grid, connect it to a **Supabase** database, and get full **create / read / update / delete** - sorting, filtering, search, and paging inclu…
- [Supabase](https://svgrid.com/enterprise/studio/supabase): Supabase gives you a hosted PostgreSQL database with a generous free tier and a browser-safe client. This guide takes you from an empty Supabase project to a live CRUD screen, step by step. No back…
- [Testing a Studio app](https://svgrid.com/enterprise/studio/testing): A Studio screen is plain SvelteKit code, so you test it with the tools you already use. Three layers, cheapest first: the **data source** (no DOM), the **screen** (a component test), and the **flow…
- [Themes & styling](https://svgrid.com/enterprise/studio/theming): Every Studio surface - the grid, the edit panel (drawer / modal / inline), the schema designer, and the generated screens - is styled with the grid's --sg-* CSS custom properties. Set the tokens on…
- [Troubleshooting & FAQ](https://svgrid.com/enterprise/studio/troubleshooting): Common issues and answers when building with SvGrid Studio.
- [Tutorial: build a CRM](https://svgrid.com/enterprise/studio/tutorial-crm): This walkthrough builds a small CRM - **Companies**, **Contacts**, and **Deals** - with SvGrid Studio: scaffolded CRUD screens, a related-record picker, a master-detail view, validation, theming, a…
- [Enterprise support](https://svgrid.com/enterprise/support): What you get on each tier, what response time to expect, how to escalate.

## Getting started

- [1. Install](https://svgrid.com/getting-started/1-install): SvGrid is a single npm package. There is no peer dependency on a CSS framework - bring your own, or use the default theme that ships with the render component.
- [2. First grid in 60 seconds](https://svgrid.com/getting-started/2-first-grid): ```svelte <script lang="ts"> import { SvGrid, type ColumnDef } from '@svgrid/grid'
- [3. Data and columns](https://svgrid.com/getting-started/3-data-and-columns): SvGrid reads two arrays: data (your rows) and columns (the column-definition list). Everything else is opt-in.
- [4. Features](https://svgrid.com/getting-started/4-features): The grid engine is **feature-gated**. Out of the box you get the core row model (rows in their original order). To enable sorting, filtering, grouping, pagination, expansion, or selection, register…
- [5. Theme and density](https://svgrid.com/getting-started/5-theme-and-density): The render component (<SvGrid>) ships its own scoped styles. You re-theme it by declaring --sg-* CSS custom properties at any level above the grid - :root for the whole app, a wrapper <div> for one…
- [6. Going to production](https://svgrid.com/getting-started/6-going-to-production): You have a working grid. This page is the checklist that turns it into something you'd ship.
- [Starters & scaffolding](https://svgrid.com/getting-started/starters): The fastest way to a working SvGrid app. One command scaffolds a project with the grid already wired up - no copy-paste, no config archaeology.

## Help / topic pages

- [Accessibility](https://svgrid.com/help/accessibility): SvGrid implements the WAI-ARIA 1.2 grid pattern with full keyboard navigation and a screen-reader announcement layer. This page documents exactly what the grid does, where the responsibility line s…
- [Build an AI agent that drives the grid](https://svgrid.com/help/agents): This is the "AI-native data grid" story end-to-end. Three patterns, in order of how much agency you hand to the LLM:
- [AI Smart Paste](https://svgrid.com/help/ai-smart-paste): Drop any shape of contact / lead / row data into a text area - CSV, TSV, JSON, vCard, Markdown table, an email signature block, or a single line of prose - and the parser maps it into typed rows yo…
- [AI assistant - Enterprise](https://svgrid.com/help/ai): Bring a language model into your grid with four helpers that stay strictly model-agnostic: **natural-language filter**, **smart fill**, **summarise**, and **classify**. Ships in the paid **@svgrid/…
- [API stability & semver policy](https://svgrid.com/help/api-stability): The promise SvGrid makes to you about which exports you can lean on, how breaking changes are communicated, and how long deprecated APIs stick around.
- [Architecture overview](https://svgrid.com/help/architecture): A one-page mental model that should let you reason about every other topic in the docs. SvGrid is a strict three-layer system - if you know which layer a piece of code lives in, you know what it ca…
- [Performance benchmarks](https://svgrid.com/help/benchmarks): Headline numbers from the regression suite. Every figure below is from the same hardware + browser configuration, re-measured on each release; the script that produces them lives in packages/grid/s…
- [Browser & runtime support](https://svgrid.com/help/browser-support): The shipping target matrix. If your environment isn't listed, assume unsupported until you've validated the linked feature surfaces below.
- [Integrated charts](https://svgrid.com/help/charts): SvGrid can chart its own data with no external charting library. Two pieces:
- [Real-time collaboration](https://svgrid.com/help/collaboration): Two people (or two AI agents) on the same grid: **presence** (who's here and where their cursor is) and **live edits** (a change in one client appears in every other). SvGrid packages this as a hea…
- [Columns hierarchy & manager](https://svgrid.com/help/columns-hierarchy): The pattern for grids with too many columns to fit on screen at once. A side-panel "column manager" exposes the column tree to the user: they pick what's visible, what gets collapsed into a one-cel…
- [Comparison: SvGrid vs AG Grid vs TanStack Table](https://svgrid.com/help/comparison): This is the page enterprise evaluators land on first. It is honest. The three projects solve overlapping problems and the right choice depends on your stack, your budget, and which constraints bite…
- [Conditional form schema](https://svgrid.com/help/conditional-form-schema): A pattern for declarative field-visibility and editability rules inside a grid. Instead of writing imperative if/else checks across many cell callbacks and editable props, you declare a **schema** …
- [Error reference](https://svgrid.com/help/errors): Every Error thrown by @svgrid/grid or @svgrid/enterprise with the exact message text, the trigger condition, and the fix. If a runtime message you see isn't on this list, it's coming from your own …
- [Data export and printing](https://svgrid.com/help/export): **CSV / TSV / JSON export and copy-to-clipboard are free** in the community **@svgrid/grid** - the commodity "data out" every grid needs. The richer formats - **Excel (xlsx)**, **legacy Excel (xls)…
- [Glossary](https://svgrid.com/help/glossary): Terminology used across the docs and the source. Sorted A-Z. If a term is unclear in a topic page and isn't on this list, please file an issue.
- [Grouping & aggregation](https://svgrid.com/help/grouping-aggregation): Roll rows up by one or more columns and compute aggregates (sum, avg, count, min, max, custom) at each group level. Powered by columnGroupingFeature plus per-column aggregator config.
- [Internationalisation & RTL](https://svgrid.com/help/i18n-rtl): The grid is locale-agnostic by design: every text label is yours to translate, every formatted value goes through Intl, and right-to-left layout flips via standard CSS logical properties.
- [Data import - Enterprise](https://svgrid.com/help/import): The sister to data export and printing. Read an Excel file, CSV/TSV blob, or JSON array in the browser and produce a typed preview of every parsed row - including per-cell validation errors - befor…
- [SvGrid Help](https://svgrid.com/help/index): Topic-oriented documentation for SvGrid. Each page is a focused explanation of one feature with copy-paste code that runs against the shipping library - written for SvGrid, not translated from anot…
- [Use sv-grid docs as LLM context](https://svgrid.com/help/llm-grounding): This page is the "how do I make ChatGPT / Claude / Cursor write good sv-grid code?" guide. Three pre-built artefacts ship with the docs specifically so models can ground themselves in current, accu…
- [MCP server](https://svgrid.com/help/mcp-server): The sv-grid MCP server lets AI clients (Claude Desktop, Cursor, Zed, Continue, custom agents) query the documentation, scaffold column definitions, and preview exports - all grounded in the same sc…
- [Migrating from AG Grid to SvGrid](https://svgrid.com/help/migrating-from-ag-grid): If you tried AG Grid on a Svelte 5 project - via ag-grid-svelte, the old ag-grid-community/svelte, or a hand-rolled wrapper - you probably hit the same friction everyone hits: the bridge between AG…
- [Migrating from DevExtreme DataGrid (DevExpress)](https://svgrid.com/help/migrating-from-devextreme): DevExtreme DataGrid is a deep, commercial multi-framework grid from DevExpress (jQuery, React, Vue, Angular). Moving to SvGrid trades the commercial suite for a Svelte-5-native grid with an MIT cor…
- [Migrating from Glide Data Grid](https://svgrid.com/help/migrating-from-glide): Glide Data Grid is the canvas-rendered, ultra-high-performance choice for very wide datasets (100k+ rows, 100+ columns). Sv-grid renders to DOM with virtualization, which trades the absolute peak F…
- [Migrating from Grid.js](https://svgrid.com/help/migrating-from-gridjs): Grid.js is a small vanilla-JS table for search, sort, and pagination that you render into a DOM element. SvGrid does the same in a Svelte-5-native component and keeps going - virtualization, Excel-…
- [Migrating from Handsontable](https://svgrid.com/help/migrating-from-handsontable): Handsontable's mental model is "spreadsheet-in-a-page": every cell editable, formulas, copy/paste with the OS clipboard, frozen rows and columns. Sv-grid covers ~90% of the same surface; the port i…
- [Migrating from jqxGrid (jQWidgets)](https://svgrid.com/help/migrating-from-jqxgrid): jqxGrid and SvGrid come from the **same team**. jqxGrid is the mature, multi-framework grid (jQuery core with Angular, React, and Vue integrations); SvGrid is the Svelte-5-native member of the fami…
- [Migrating from Kendo UI Grid (Telerik)](https://svgrid.com/help/migrating-from-kendo-grid): Kendo UI Grid is the polished, commercial enterprise grid from Telerik / Progress, available for jQuery, React, Vue, and Angular. Moving to SvGrid trades the multi-framework commercial suite for a …
- [Migrating from MUI X DataGrid](https://svgrid.com/help/migrating-from-mui-x): MUI X DataGrid is the most common starting point for teams already on Material UI. It's a closed-source-Pro / open-source-Community split very similar to sv-grid's. The port is mostly mechanical.
- [Migrating from PrimeVue / PrimeNG / PrimeReact DataTable](https://svgrid.com/help/migrating-from-primevue-datatable): The Prime DataTable is the grid in the Prime UI suites for Vue, Angular, and React. Its columns are declared as <Column> children with field / header props - a shape that maps cleanly onto SvGrid's…
- [Migrating from React Data Grid (adazzle)](https://svgrid.com/help/migrating-from-react-data-grid): react-data-grid is a fast, Excel-like React grid with a clean column API. SvGrid mirrors its shape - virtualized DOM, typed columns, inline editing, custom cells - on Svelte 5 runes instead of Reac…
- [Migrating from Smart.Grid (Smart Web Components)](https://svgrid.com/help/migrating-from-smart-grid): Smart.Grid (htmlelements.com) and SvGrid come from the **same team**. Smart.Grid is a framework-agnostic custom element - it even works inside Svelte today - while SvGrid is the reactive, Svelte-5-…
- [Migrating from SVAR Svelte DataGrid](https://svgrid.com/help/migrating-from-svar-svelte-datagrid): SVAR Svelte DataGrid (wx-svelte-grid) is, like SvGrid, an actual Svelte-native grid rather than a wrapper, and both are MIT-licensed and free for commercial use. Because both are component-first wi…
- [Migrating from svelte-headless-table](https://svgrid.com/help/migrating-from-svelte-headless-table): svelte-headless-table popularised the headless-table pattern in the Svelte ecosystem. It is built on Svelte 4 stores and plugins (addSortBy, addColumnFilters, addPagination, ...), and it leaves the…
- [Migrating from Syncfusion Grid](https://svgrid.com/help/migrating-from-syncfusion): Syncfusion Grid is a comprehensive commercial grid across JavaScript, React, Vue, Angular, and Blazor, with a free community license for qualifying small teams. Moving to SvGrid trades the multi-fr…
- [Migrating from Tabulator](https://svgrid.com/help/migrating-from-tabulator): Tabulator is a feature-rich vanilla-JS table you instantiate against a DOM element and drive imperatively (table.setData(...), table.on('cellEdited', ...)). SvGrid covers a similar feature surface …
- [Migrating from TanStack Table](https://svgrid.com/help/migrating-from-tanstack-table): TanStack Table (formerly React Table v8) is the closest conceptual sibling to sv-grid - both are "headless data grid" libraries with explicit row-model pipelines. If you already think in createTabl…
- [Migrating from a UI-kit table (Flowbite / Skeleton / shadcn-svelte)](https://svgrid.com/help/migrating-from-ui-kit-tables): Flowbite-Svelte, Skeleton, and shadcn-svelte all ship a styled <Table> component. They are presentation components: they render the rows you pass and match your design tokens, but sorting, filterin…
- [Migrating from @vincjo/datatables](https://svgrid.com/help/migrating-from-vincjo-datatables): @vincjo/datatables is a small, ergonomic Svelte datatable helper: you wrap your data in a handler and it gives you reactive sorting, filtering, and pagination while you keep your own table markup. …
- [Missing features](https://svgrid.com/help/missing-features): An honest accounting of what is **not yet built**, audited against the shipped demo catalog. Most of what used to live here has shipped; the remaining gaps are small and clearly marked. Each entry …
- [Mobile card view](https://svgrid.com/help/mobile-card-view): On wide screens the grid is the grid. Under a viewport breakpoint (by convention 720 px), the same $state array re-renders as touch-friendly cards. Tap a card to expand it into an edit panel; the w…
- [Observability](https://svgrid.com/help/observability): Production-grade visibility into a live grid: which rows users see, which cells they edit, what they filter for, how often the imperative API is invoked. Everything you need to wire into Sentry, Da…
- [Pivot tables - Enterprise](https://svgrid.com/help/pivot): A built-in pivot model that turns a flat data set into a row-axis tree, a nested column-axis header, and one aggregated cell per (row-path × col-path × measure) triple. Ships in **@svgrid/enterpris…
- [Production deployment](https://svgrid.com/help/production): The checklist that turns "it works on my laptop" into "it ships". One page per concern; each concern is one paragraph + the code that matters. <div data-docs-demo="22-admin-template" data-height="5…
- [Real-time / streaming updates](https://svgrid.com/help/real-time): How to drive the grid from a WebSocket / SSE / poll. Three patterns ranked by the rate of change:
- [Recipes / Cookbook](https://svgrid.com/help/recipes): Quick patterns for the questions that come up over and over. Each recipe is paired with a live demo or a minimal snippet that runs against the shipping library.
- [Saved views & layout persistence](https://svgrid.com/help/saved-views): The pattern for "user changes sort + filters + columns; reopens the page next week; everything is where they left it." Powered entirely by your own state - the grid never writes to storage on its own.
- [Security & supply chain](https://svgrid.com/help/security): SvGrid is built for shipping into regulated environments where every dependency, every runtime call, and every network egress has to be justified before procurement signs. This page is the answer t…
- [Server-side data](https://svgrid.com/help/server-side-data): The patterns for moving sort, filter, group, and pagination off the client and onto your API. Three flavours, ranked by complexity:
- [Spreadsheet formulas](https://svgrid.com/help/spreadsheet-formulas): A minimal Excel-style formula engine that runs in the browser, with no dependencies. Cells can hold either a literal value (number, string, boolean) or a formula starting with =. When the formula r…
- [State maintenance](https://svgrid.com/help/state-maintenance): The pattern for snapshotting every dimension the user can change about the grid - sort, filters, group-by, column visibility + widths, pagination, expansion, selection, active cell - into a single …
- [Status bar](https://svgrid.com/help/status-bar): The status bar is the strip under the grid that shows live aggregates of the **selected cell range** - count, sum, average, min, max - the way Excel does at the bottom-right when you select a block…
- [Tailwind integration](https://svgrid.com/help/tailwind): SvGrid was built alongside Tailwind v4 (the gallery is the proof). The two compose cleanly because they have **non-overlapping concerns**: <div data-docs-demo="10-custom-cells-and-themes" data-heig…
- [Testing & Quality](https://svgrid.com/help/testing-and-quality): SvGrid ships with a comprehensive automated test suite. This page is the honest accounting of what we test, what we don't, and where coverage stands today.
- [Testing your grid](https://svgrid.com/help/testing): How to write tests that catch regressions before they ship. SvGrid is designed for both **fast unit tests** (Layer 2, the headless engine, runs in pure node) and **slow but accurate browser tests**…
- [Design tokens](https://svgrid.com/help/tokens): Every visual property the grid exposes for theming is a CSS custom property (--sg-*). Tokens cascade like any other CSS variable - override at :root, on a wrapper element, or per-grid; the closest …
- [Doc versioning](https://svgrid.com/help/versioning): How sv-grid documentation tracks multiple shipping major versions.
- [Web Components & Custom Elements](https://svgrid.com/help/web-components): Ship SvGrid as a framework-agnostic web component (custom element) and use the Svelte 5 data grid in React, Vue, Angular, or plain HTML - no Svelte required in the host app.

## Cells

- [Cell components](https://svgrid.com/help/cells/cell-components): For any cell whose content is more than a string, use cell: with renderSnippet or renderComponent. <div data-docs-demo="10-custom-cells-and-themes" data-height="540"></div>
- [Cell data types](https://svgrid.com/help/cells/cell-data-types): The editorType field on a column tags the column with a type. This drives three different behaviours: <div data-docs-demo="01-quick-start" data-height="540"></div>
- [Cell text selection](https://svgrid.com/help/cells/cell-text-selection): By default the grid has **cell-range selection** (drag across cells, range highlight, copy as TSV). Standard browser text-selection inside a cell is **not** the default - clicking-and-dragging acro…
- [Conditional formatting](https://svgrid.com/help/cells/conditional-formatting): Conditional formatting colors a cell by its value. SvGrid ships it as a declarative engine prop, conditionalFormats, so you describe the rules once and the grid paints every cell - no per-cell cell…
- [Expressions](https://svgrid.com/help/cells/expressions): SvGrid does not ship a formula / expression language for cells. Computed values are JavaScript - either via fieldFn or inside a cell callback. <div data-docs-demo="83-spreadsheet-formulas" data-hei…
- [Getting values](https://svgrid.com/help/cells/getting-values): You get cell values in three ways depending on context. <div data-docs-demo="04-selection-copy-paste" data-height="540"></div>
- [Highlighting changes](https://svgrid.com/help/cells/highlighting-changes): Set cellFlash on a column and the grid flashes that cell whenever its value changes - edits, streaming feeds, server pushes. It is keyed by row identity, so scrolling a virtualized grid (which recy…
- [Sparklines](https://svgrid.com/help/cells/sparklines): A sparkline is a tiny, word-sized chart drawn inside a single cell. SvGrid renders them as a first-class column type: set sparkline on a column whose value is an array of numbers and the grid paint…
- [Styling cells](https://svgrid.com/help/cells/styling-cells): Cells render as <td> and pick up the same CSS variable tokens as the rest of the grid. <div data-docs-demo="62-conditional-styling" data-height="540"></div>
- [Text formatting](https://svgrid.com/help/cells/text-formatting): The format field on a column produces locale-aware formatted strings without you writing a renderer. <div data-docs-demo="13-finances" data-height="540"></div>
- [Tooltips](https://svgrid.com/help/cells/tooltips): There is no built-in tooltip API on ColumnDef. Use the standard title attribute, an accessible <dialog>, or any popover library - wired through a custom cell renderer.
- [View refresh](https://svgrid.com/help/cells/view-refresh): The grid renders reactively - it does **not** have a refresh() method, because it doesn't need one. To make the grid re-display, change the data that drives it.

## Columns

- [Aligned grids](https://svgrid.com/help/columns/aligned-grids): Aligned grids are two or more independent <SvGrid> instances kept in lockstep: **horizontal scroll** and **column-resize widths** propagate between them. Use it for a budget-vs-actuals comparison, …
- [Column definitions](https://svgrid.com/help/columns/column-definitions): A ColumnDef tells SvGrid how to read a value out of a row, how to render it, and which features apply to it. The grid below is built from a handful of ColumnDefs - look at the source to see how eac…
- [Column groups](https://svgrid.com/help/columns/column-groups): A column group is a ColumnDef whose columns array contains children. The parent renders a spanning header above its children. The pivot demo below shows three levels of grouped headers in action - …
- [Column headers - styling & height](https://svgrid.com/help/columns/column-headers): Headers are rendered as <th> inside a <thead> with role="row". Style them with regular CSS - there are no header-specific Svelte props. <div data-docs-demo="01-quick-start" data-height="540"></div>
- [Column menu](https://svgrid.com/help/columns/column-menu): Every column header carries a menu button (⋮). By default it's a flat list of actions plus a "Choose columns" submenu. Opt into the AG-Grid **tabbed** layout with columnMenuTabs:
- [Column moving (drag to reorder)](https://svgrid.com/help/columns/column-moving): There are two ways to move columns in sv-grid:
- [Column pinning](https://svgrid.com/help/columns/column-pinning): Pinning sticks a column to the **left** or **right** edge of the viewport so it does not scroll horizontally with the rest.
- [Column sizing](https://svgrid.com/help/columns/column-sizing): Each column has a pixel width. The default for all columns is the grid's columnWidth prop (default ~140 px); each column can override via its width field. <div data-docs-demo="63-column-layout-api"…
- [Column & row spanning (merged cells)](https://svgrid.com/help/columns/column-spanning): Spanning lets a single body cell cover **multiple columns** and/or **rows** - merged report headers, grouped labels, financial statements. SvGrid does this with a real colspan / rowspan merge engin…
- [Column state](https://svgrid.com/help/columns/column-state): "Column state" is the bag of per-column settings that change at runtime: visibility, width, pinning, sort, filter. SvGrid keeps these as separate state slices inside the grid instance. <div data-do…
- [Custom header components](https://svgrid.com/help/columns/custom-header-components): The header field on a ColumnDef accepts a function that returns either a renderSnippet(...) or renderComponent(...). The function receives a HeaderContext so it can access the column, header, and g…
- [Tool panel (Columns + Filters)](https://svgrid.com/help/columns/tool-panel): The tool panel is the docked sidebar - standard in enterprise grids - for managing columns and filters without hunting through a right-click menu. Turn it on with the toolPanel prop:
- [Updating column definitions](https://svgrid.com/help/columns/updating-definitions): There are two ways to change columns after the grid has mounted:

## Editing

- [Edit components](https://svgrid.com/help/editing/edit-components): The grid ships with five inline editors. Each is selected by the column's editorType: <div data-docs-demo="66-custom-cell-editors" data-height="540"></div>
- [Full-row editing](https://svgrid.com/help/editing/full-row): Full-row editing opens an **entire row** for edit at once: every editable cell shows an inline editor, a single Enter (or clicking away) commits all of them in one update, and Esc cancels the whole…
- [Editing - overview](https://svgrid.com/help/editing/overview): Inline editing is a single prop on <SvGrid>. Try it - double-click any cell, type to replace, hit Enter to commit. Tab moves to the next editable cell:
- [Parsing values](https://svgrid.com/help/editing/parsing-values): When the user commits an edit, the editor's DOM value (a string for text / number / date inputs, a boolean for checkboxes) is parsed by parseEditorValue into the canonical value for the column's ty…
- [Provided cell editors](https://svgrid.com/help/editing/provided-editors): The grid ships nine built-in editors, selected via editorType on the column definition. Each editor renders inside the cell while editing and falls back to the default text render (or your cell sni…
- [Saving values](https://svgrid.com/help/editing/saving-values): When the user commits an edit, the new value is written into the grid's **internal data copy**. The grid does **not** mutate the array you passed in via the data prop - it keeps its own working cop…
- [Start / stop editing](https://svgrid.com/help/editing/start-stop-editing): The cell must be **focused** (the grid's active cell). Click any cell or use the arrow keys to set focus.
- [Undo / redo](https://svgrid.com/help/editing/undo-redo): There is **no built-in undo/redo stack** inside <SvGrid>. The grid emits no per-edit event for an external stack to subscribe to, so undo/redo has to be built on top of the data-snapshot approach. …
- [Validation](https://svgrid.com/help/editing/validation): There is no validate(value) callback on ColumnDef today. Validation happens by intercepting committed edits and either accepting or reverting them.

## Filtering

- [Applying filters](https://svgrid.com/help/filtering/applying-filters): "Applying" a filter means running it against the data. SvGrid applies filters **eagerly** by default: every keystroke updates the row model and the visible rows (with a 150ms debounce inside the co…
- [Custom column filters](https://svgrid.com/help/filtering/custom-column-filters): When the built-in operators and the set-filter pattern are not enough, take over the filter pipeline. <div data-docs-demo="03-excel-filters" data-height="540"></div>
- [Date filter](https://svgrid.com/help/filtering/date-filter): A column with editorType: 'date' (or 'datetime') gets the **date** filter operator set: equals, lessThan, greaterThan, **between**, isBlank. <div data-docs-demo="64-filter-between-operator" data-he…
- [Filter API](https://svgrid.com/help/filtering/filter-api): Two surfaces - pick based on whether the caller is inside or outside the component that owns the grid state. <div data-docs-demo="64-filter-between-operator" data-height="540"></div>
- [Filter conditions](https://svgrid.com/help/filtering/filter-conditions): A "filter condition" is a (column, operator, value) triple. The grid stores filter conditions in state.columnFilters. <div data-docs-demo="03-excel-filters" data-height="540"></div>
- [Floating filters](https://svgrid.com/help/filtering/floating-filters): "Floating filters" are the always-visible filter inputs that sit **between** the header row and the body. In SvGrid the filter row is **per-operator and per-type**: each cell has a funnel to pick t…
- [Number filter](https://svgrid.com/help/filtering/number-filter): A column with editorType: 'number' gets the **number** filter operator set: equals, greaterThan, lessThan, **between**, isBlank. The default operator is equals. <div data-docs-demo="64-filter-betwe…
- [Filtering - overview](https://svgrid.com/help/filtering/overview): Click any column header's filter icon to open the operator + value popover; numeric and date columns range-bucket their distinct values so the menu stays usable on big datasets:
- [Set filter](https://svgrid.com/help/filtering/set-filter): A "set filter" (a.k.a. value filter, list filter) shows a checklist of all distinct values in a column and lets the user pick which to include. It's what you reach for to filter status to "active O…
- [Text filter](https://svgrid.com/help/filtering/text-filter): A column with no editorType (or editorType: 'text') gets the **text** filter operator set: contains, equals, startsWith, isBlank. The default operator is contains. <div data-docs-demo="69-highlight…

## help/grouping

- [Group aggregators](https://svgrid.com/help/grouping/aggregators): When grouping is active, each group row can show a rolled-up value per column - the sum of revenue, the average score, the count of deals. SvGrid does this declaratively: set aggregate on a column …

## help/headless

- [Build a table from scratch](https://svgrid.com/help/headless/build-a-table): This is the whole point of headless: the engine computes the rows, **you** emit the markup. Here's a complete, sortable, filterable <table> in one Svelte component - no <SvGrid>, no grid CSS.
- [Controlled state](https://svgrid.com/help/headless/controlled-state): The engine never owns its state - **you do**. You pass state in and the engine calls your onXxxChange handlers with the next value. That one rule is what makes headless SvGrid click with Svelte 5 $…
- [Headless overview](https://svgrid.com/help/headless/overview): <SvGrid> is the **renderer**. createSvGrid is the **engine** that powers it. They're independent: you can use the engine on its own to build a custom UI, render a plain <table> for print or email, …
- [Row models](https://svgrid.com/help/headless/row-models): The engine turns your raw data into the rows you render by running it through a **pipeline** of row models. You opt into the steps you need; everything else is tree-shaken out.
- [Server-side data (load on demand)](https://svgrid.com/help/headless/server-side): When the rows live behind an API and there are too many to ship to the browser, the server does the work: **sorting**, **filtering**, and **paging** become query params, and it returns only the cur…
- [Styling a headless table](https://svgrid.com/help/headless/styling): With the headless engine you render your own markup, so **you own every pixel of styling** - there is no grid stylesheet to fight. The same createSvGrid engine can drive a minimal underlined table,…
- [Headless virtualization](https://svgrid.com/help/headless/virtualization): <SvGrid> virtualizes rows and columns for you. When you render your own markup, you virtualize yourself with the same engine SvGrid uses - createSvelteVirtualizer (reactive) or createVirtualizer (f…

## Rows

- [Accessing rows](https://svgrid.com/help/rows/accessing-rows): You can read the grid's current rows in three ways. <div data-docs-demo="04-selection-copy-paste" data-height="540"></div>
- [Full-width rows](https://svgrid.com/help/rows/full-width-rows): "Full-width rows" are rows whose content takes the entire grid width instead of being cell-by-cell - useful for inline editors, banner ads, section dividers, and detail-row expansions. <div data-do…
- [Master / detail (nested grids)](https://svgrid.com/help/rows/master-detail): Master/detail is the AG-Grid pattern where each **master** row expands to reveal a **detail** panel - most often a nested grid of the row's child records. SvGrid builds it from two props on <SvGrid>:
- [Row data](https://svgrid.com/help/rows/row-data): Row data is whatever you pass to <SvGrid data={...}>. It is a ReadonlyArray<TData> where TData is your row type. Any shape works; the grid does not require a base class or interface. <div data-docs…
- [Row dragging](https://svgrid.com/help/rows/row-dragging): Managed row dragging lets users **reorder rows** within a grid and **move rows between grids**. Turn on rowDragManaged and every row becomes a drag source (grab cursor + a grip in the row-number ce…
- [Row height](https://svgrid.com/help/rows/row-height): Row height is a single integer in pixels. <div data-docs-demo="10-custom-cells-and-themes" data-height="540"></div>
- [Row pagination](https://svgrid.com/help/rows/row-pagination): Pagination is opt-in. Register the feature, register the row model, then either turn it on with showPagination or drive its state from outside. <div data-docs-demo="02-sort-filter-paginate" data-he…
- [Row pinning (top / bottom)](https://svgrid.com/help/rows/row-pinning): Pin specific rows to the **top** or **bottom** of the grid so they stay visible while the rest of the data scrolls. Typical use cases:
- [Row sorting](https://svgrid.com/help/rows/row-sorting): Sorting is a feature you opt into. Try it live - click any column header to cycle none → asc → desc → none; shift-click adds the column to the sort key list (multi-sort):
- [Row spanning](https://svgrid.com/help/rows/row-spanning): "Row spanning" lets one cell's content cover **multiple rows**, the way a merged cell does in a spreadsheet.
- [Styling rows](https://svgrid.com/help/rows/styling-rows): Rows are <tr role="row"> elements inside the grid table. Style them with plain CSS. <div data-docs-demo="62-conditional-styling" data-height="540"></div>
- [Transactions](https://svgrid.com/help/rows/transactions): api.applyTransaction({ add, update, remove }) applies a batch of row mutations in a **single** data update - one re-render for the whole batch, not one per row. It's the path to use for high-freque…
- [Tree rows (expand / collapse)](https://svgrid.com/help/rows/tree-rows): SvGrid does not have a treeData prop. Tree-shaped data renders through the same data + columns pipeline as any other grid, with the tree behaviour living in *your* derived-state code. This is on pu…

## help/server

- [Server-Side Row Model (SSRM)](https://svgrid.com/help/server/server-row-model): When the data lives on the server - millions of rows in a database - the grid should hold only the page on screen and push sorting, filtering, and paging to the backend. SvGrid packages this as **o…

## help/state

- [Named views](https://svgrid.com/help/state/named-views): A "view" is a saved snapshot of the grid's state - sort, filters, column order/width/visibility, page, grouping. Named views let a user save the layout they like and switch between them. SvGrid shi…

## help/ui-components

- [Buttons & toggles](https://svgrid.com/help/ui-components/buttons): Press and toggle primitives. All take a size (sm | md | lg) and theme from --sg-*.
- [Date & time](https://svgrid.com/help/ui-components/date-time): Three date/time controls built on a shared, framework-free date engine (date-core, date-format, date-selection, date-restrict). They are the rich-by-default editors for date, datetime and time grid…
- [UI components (SvGrid Editors)](https://svgrid.com/help/ui-components/index): SvGrid ships a suite of Svelte 5 UI components in the free @svgrid/grid package. Every one is **two things at once**:
- [Inputs](https://svgrid.com/help/ui-components/inputs): Typed text controls. Each emits a clean value and themes from --sg-*.
- [Layout & composite](https://svgrid.com/help/ui-components/layout): A WAI-ARIA tabs widget with roving tabindex and arrow-key navigation. The active panel is rendered through the panel snippet (receives the active id).
- [Range & feedback](https://svgrid.com/help/ui-components/range): A single or dual-thumb range slider (ARIA slider) with steps, tick marks, keyboard and pointer drag; horizontal or vertical.
- [Selection](https://svgrid.com/help/ui-components/selection): List and overlay pickers. The overlay controls portal their panel to <body> (carrying theme tokens) so they never clip, and share a common option shape:

## legal

- [SVGRID END USER LICENSE AGREEMENT](https://svgrid.com/legal/EULA): Version 1.0

## Recipes / cookbook

- [Auto-save edits with debounce](https://svgrid.com/recipes/autosave-with-debounce): Persist every cell edit to the server, debounced so a fast typer generates one save, not twenty.
- [Barcode cells in a Svelte data grid (EAN-13)](https://svgrid.com/recipes/barcode-cells): Render a real, scannable EAN-13 barcode in every row of your Svelte data grid - drawn as crisp SVG with no canvas, no eval, and no third-party dependency. This is the retail, warehouse, and invento…
- [Benchmark harness](https://svgrid.com/recipes/benchmark-harness): You're answering:
- [Between-operator filters for date ranges](https://svgrid.com/recipes/between-date-filter): "From / to" date range as one filter, not two.
- [Build your own feature plugin](https://svgrid.com/recipes/build-a-feature-plugin): You want to inject behavior that applies to *every* row or column - a computed accent class, an audit fingerprint, an inline diff badge, a per-row policy gate - without forking the grid component o…
- [Bulk-edit selected rows](https://svgrid.com/recipes/bulk-edit-selected-rows): Goal: the user selects N rows, opens a toolbar, picks a field + value, and **every** selected row updates in one click. Standard back-office workflow for status changes, reassignments, tag applicat…
- [Cell renderer patterns](https://svgrid.com/recipes/cell-renderer-patterns): Every supported shape for the cell: slot on a ColumnDef, with the trade-offs for each.
- [Chart.js sync from grid filter state](https://svgrid.com/recipes/chartjs-sync): A chart that re-renders from api.getDisplayedRows() on every filter / sort change.
- [Conditional row coloring](https://svgrid.com/recipes/conditional-row-coloring): rowClass returns a class map per row; CSS does the rest.
- [Cursor-based infinite scroll](https://svgrid.com/recipes/cursor-based-infinite-scroll): Sparse-load a 100k-row audit log without paginating. Pattern: debounce on scroll, fetch the next chunk by cursor, splice into $state.
- [Custom filter functions](https://svgrid.com/recipes/custom-filter-functions): You need any of:
- [Dependent dropdowns (cascading editors)](https://svgrid.com/recipes/dependent-dropdowns): Country → State → City. Editing one level resets downstream levels; each cell's dropdown options come from the row's upstream value.
- [Drag-drop columns to reorder](https://svgrid.com/recipes/drag-drop-columns): Re-order columns by dragging them in a side panel; live grid reflects the new order.
- [External search box with highlighted matches](https://svgrid.com/recipes/external-search): A search input outside the grid filters the visible rows AND highlights matched substrings inline.
- [Form library bridge](https://svgrid.com/recipes/form-library-bridge): You want the same row to be editable two ways:
- [Grid state inspector](https://svgrid.com/recipes/grid-state-inspector): You're stuck on one of:
- [Heatmap-tinted numeric cells](https://svgrid.com/recipes/heatmap-cells): Bucket cell values into red/amber/neutral/green tints based on per-column min/max.
- [Recipes / cookbook](https://svgrid.com/recipes/index): Copy-paste patterns for sv-grid. Every recipe is a complete, runnable snippet you can drop into your app. Each pairs with a live demo in the gallery.
- [Kanban board over the same `$state`](https://svgrid.com/recipes/kanban-from-grid-data): One <SvGrid> with columns = lanes, rows pivoted by status. HTML5 drag-drop moves cards between lanes.
- [Lazy-load expand on demand](https://svgrid.com/recipes/lazy-load-expand): Tree rows where children are fetched only when the user expands their parent.
- [Loading data from REST + GraphQL](https://svgrid.com/recipes/loading-data): Three shapes. Pick the one that matches your back-end; the grid doesn't care which.
- [1 million rows with virtualization](https://svgrid.com/recipes/million-rows): Row + column virtualization configured for very wide / very tall data.
- [Mobile card view with grid-on-desktop](https://svgrid.com/recipes/mobile-card-pivot): Same data, two layouts. Auto-pivot below 720px or force one via a toolbar switch.
- [Multi-sheet xlsx export](https://svgrid.com/recipes/multi-sheet-export): One workbook, multiple tabs - one per group / region / period.
- [NL filter wired to your LLM](https://svgrid.com/recipes/nl-filter): "show last quarter > $10k sorted by date" - one natural-language input drives setFilter + setSort.
- [Persist column layout to URL](https://svgrid.com/recipes/persist-column-layout-to-url): Goal: when a user sorts, filters, or pages the grid, the URL updates so that copy-pasting the link gives the recipient the same view. On reload the grid restores from the URL.
- [Pin first column on horizontal scroll](https://svgrid.com/recipes/pin-first-column): Wide grids where the row-label column must stay visible while the data scrolls horizontally.
- [Profiling with a live FPS HUD](https://svgrid.com/recipes/profiling-with-fps-hud): You're trying to figure out:
- [Role-based editable columns](https://svgrid.com/recipes/role-based-editing): Different user roles can edit different cells; the grid hides editors per-row via the editable callback.
- [Saved filter sets](https://svgrid.com/recipes/saved-filter-sets): Name and recall named filter combinations; round-trip via api.getFilters / setFilter.
- [Saved views (localStorage)](https://svgrid.com/recipes/saved-views): Per-user grid layouts. Each "view" snapshots column widths, pinning, sort, filter, page index, and (optionally) grouping. Switching views restores the snapshot.
- [Server-side filter with TanStack Query](https://svgrid.com/recipes/server-side-filter-with-tanstack-query): Goal: drive the grid from a real API. Sort + filter + page round-trip to the server; the response goes into TanStack Query so we get **caching, deduplication, request cancellation, retries, optimis…
- [Smart paste: CSV / TSV / free-form → typed rows](https://svgrid.com/recipes/smart-paste): Paste anything; the assistant infers columns and previews typed rows for accept/update/skip.
- [Sparkline cell renderer](https://svgrid.com/recipes/sparkline-cells): In-cell mini-charts of a value series, rendered via inline SVG.
- [Submit-time validation with error summary](https://svgrid.com/recipes/submit-time-validation): Different from per-keystroke validation. The user edits freely; on **Submit**, a row-level validator runs and highlights every invalid cell with an aria-live summary panel.
- [Testing your grid](https://svgrid.com/recipes/testing-your-grid): Three strategies for testing a grid in your app. Pick the strategy that matches the question you're answering.
- [Theme-matched xlsx export](https://svgrid.com/recipes/theme-matched-export): Excel file colours follow the same --sg-* tokens the grid renders with.
- [Theme tokens for Ant / MUI / Fluent / shadcn](https://svgrid.com/recipes/theme-presets): Map your design system's tokens to --sg-* and the grid re-skins.
- [Two-grid master / detail](https://svgrid.com/recipes/two-grid-master-detail): Goal: one grid shows the **master** (parents - orders, customers, projects, …), a second grid below shows the **detail** for whichever row is selected (line items, contacts, tasks, …).
- [Undo / redo for grid edits](https://svgrid.com/recipes/undo-redo-edits): Maintain a history stack of cell edits; Ctrl+Z / Ctrl+Y replay backwards / forwards.
- [Use SvGrid from React (custom-element bridge)](https://svgrid.com/recipes/use-svgrid-from-react): SvGrid is Svelte-5-native, but you can drop it into a React app via a thin **custom element** wrapper. The wrapper publishes one <sv-grid-element> tag, React renders it like any other DOM element, …
- [WebSocket streaming with backpressure](https://svgrid.com/recipes/websocket-streaming): Live tick stream with pause / resume / disconnect-and-replay and per-tick delta merge.

## API reference

- [Auto-generated reference](https://svgrid.com/reference/auto/index): _Generated from the published TypeScript sources. Do not edit by hand - rerun node tools/build-reference.mjs._
- [`@svgrid/enterprise` · `ai.ts`](https://svgrid.com/reference/auto/svgrid-enterprise-ai): Auto-generated. Source: packages\enterprise\src\ai.ts.
- [`@svgrid/enterprise` · `export.ts`](https://svgrid.com/reference/auto/svgrid-enterprise-export): Auto-generated. Source: packages\enterprise\src\export.ts.
- [`@svgrid/enterprise` · `import.ts`](https://svgrid.com/reference/auto/svgrid-enterprise-import): Auto-generated. Source: packages\enterprise\src\import.ts.
- [`@svgrid/enterprise` · `install.ts`](https://svgrid.com/reference/auto/svgrid-enterprise-install): Auto-generated. Source: packages\enterprise\src\install.ts.
- [`@svgrid/enterprise` · `pivot.ts`](https://svgrid.com/reference/auto/svgrid-enterprise-pivot): Auto-generated. Source: packages\enterprise\src\pivot.ts.
- [`@svgrid/grid` · `core.ts`](https://svgrid.com/reference/auto/svgrid-grid-core): Auto-generated. Source: packages\grid\src\core.ts.
- [`@svgrid/grid` · `svgrid-wrapper.types.ts`](https://svgrid.com/reference/auto/svgrid-grid-svgrid-wrapper.types): Auto-generated. Source: packages\grid\src\svgrid-wrapper.types.ts.
- [Bundle size](https://svgrid.com/reference/bundle-size): What each feature costs in the final bundle, how to reproduce the numbers on your branch, and what to do if size matters.
- [`ColumnDef` reference](https://svgrid.com/reference/ColumnDef): A column definition is the contract between the grid and one column of data. Pass an array of these as the columns prop to <SvGrid>.
- [Enterprise reference](https://svgrid.com/reference/enterprise): Everything that @svgrid/enterprise adds on top of the Community surface. Install + license: see Enterprise feature pack.
- [Features reference](https://svgrid.com/reference/features): A "feature" is a small bundle of state + helpers the grid engine checks for when wiring its row-model pipeline. Register only what you use - each feature ships about 1-2 KB gzipped and adds a small…
- [Headless engine](https://svgrid.com/reference/headless-engine): createSvGrid(options) returns a table object with:
- [API reference](https://svgrid.com/reference/index): The exhaustive surface of the published packages. For walkthroughs + patterns see Help; for first contact see Getting started.
- [`<SvGrid>` reference](https://svgrid.com/reference/SvGrid): The render component. One <SvGrid> element per grid instance.
- [`SvGridApi` reference](https://svgrid.com/reference/SvGridApi): The imperative API exposed via <SvGrid onApiReady={(api) => ...}>. Use it for data, column, filter, sort, group, selection, and visibility operations from outside the component.
